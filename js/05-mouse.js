/*
 * Події миші
 * - mouseenter і mouseleave (це ховер)
 * - mousemove (chatty event - балакуча подія)
 */

// const boxRef = document.querySelector('.js-box');
// const childEl = boxRef.firstElementChild;

// boxRef.addEventListener('mouseenter', e => {
//   console.log(e);

//!======================================================

//   boxRef.classList.add('box--active');
//   childEl.style.left = `${e.layerX - 50}px`;
//   childEl.style.top = `${e.layerY - 50}px`;
// });

// boxRef.addEventListener('mouseleave', () => {
//   boxRef.classList.remove('box--active');
// });

//!======================================================

// boxRef.addEventListener('mousemove', e => {
//   childEl.style.left = `${e.x - 50}px`;
//   childEl.style.top = `${e.y - 100}px`;
// });
